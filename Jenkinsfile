pipeline {
    agent {
      dockerfile {
        filename 'Dockerfile'
        label 'master-slave'
      }
    }

    stages {
      stage('Install ArgoCD in the Cluster') {
        steps {

        sh '''
             mkdir dhdhd
             '''

        }
     }
   }
}

